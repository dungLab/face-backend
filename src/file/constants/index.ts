// s3
export enum S3BucketType {
  FACE = 'face-album',
}

export enum FolderType {
  PHOTO = 'photo',
  PROFILE = 'profile',
}

export enum FileType {
  IMAGE = 'image',
  DOCS = 'docs',
}

export enum FileMetaType {
  URL = 'url',
  PUBLIC_ID = 'public_id',
}
